# Average Speed Program

distance = float(input("Enter distance (km): "))
time = float(input("Enter time (hours): "))

average_speed = distance / time

print("Average speed is:", average_speed, "km/h")